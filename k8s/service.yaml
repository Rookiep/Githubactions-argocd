apiVersion: v1
kind: Service
metadata:
  name: nginx-app
  labels:
    app: nginx-app
spec:
  type: ClusterIP  # Changed from ClusterIP to NodePort
  selector:
    app: nginx-app
  ports:
    - name: http
      port: 80
      targetPort: 80